This folder is a copy of `../web/` for GitHub Pages (which serves a branch +
folder, not an arbitrary path). Edit `web/`, then re-sync with the resync
script described in the main README under "Publish the website".
